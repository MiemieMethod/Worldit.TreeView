<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Default="clr-namespace:Worldit.TreeView">

    <Style x:Key="ExpandCollapseToggleStyle"
           TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable"
                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Width="9"
                            Height="9"
                            BorderThickness="1"
                            BorderBrush="{Binding ExpanderBorderBrush, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}"
                            CornerRadius="1"
                            SnapsToDevicePixels="True"
                            Background="{Binding ExpanderBackground, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}">
                        <Path Name="ExpandPath"
                              Margin="1,1,1,1"
                              Fill="{Binding ExpanderGlyphBrush, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}"
                              Data="M 0 2 L 0 3 L 2 3 L 2 5 L 3 5 L 3 3 L 5 3 L 5 2 L 3 2 L 3 0 L 2 0 L 2 2 Z" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked"
                                 Value="True">
                            <Setter Property="Data"
                                    TargetName="ExpandPath"
                                    Value="M 0 2 L 0 3 L 5 3 L 5 2 Z" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Default:InsertMarker}">
        <Setter Property="IsHitTestVisible"
                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Default:InsertMarker}">
                    <Grid>
                        <Border VerticalAlignment="Center"
                                Height="2"
                                Background="{TemplateBinding MarkerBrush}" />
                        <Path Data="m 0 0 l 3 3 l -3 3"
                              Fill="{TemplateBinding MarkerBrush}"
                              HorizontalAlignment="Left" />
                        <Path Data="m 0 0 l -3 3 l 3 3"
                              Fill="{TemplateBinding MarkerBrush}"
                              HorizontalAlignment="Right" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Default:EditTextBox}">
        <Setter Property="Foreground"
                Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="KeyboardNavigation.TabNavigation"
                Value="None" />
        <Setter Property="HorizontalContentAlignment"
                Value="Left" />
        <Setter Property="FocusVisualStyle"
                Value="{x:Null}" />
        <Setter Property="AllowDrop"
                Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Default:EditTextBox}">
                    <Border Background="{Binding EditBackground, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}"
                            BorderThickness="1"
                            BorderBrush="{Binding EditBorderBrush, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}"
                            Padding="0 1 2 0">
                        <ScrollViewer Name="PART_ContentHost"
                                      TextElement.Foreground="{Binding EditForeground, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SharpTreeViewScrollViewerStyle"
           TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background"
                Value="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                Content="{TemplateBinding Content}"
                                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Margin="{TemplateBinding Padding}" />
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Row="0"
                                   Grid.Column="1"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Row="1"
                                   Grid.Column="0"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Orientation="Horizontal"
                                   Value="{TemplateBinding HorizontalOffset}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                        <Rectangle Grid.Row="1"
                                   Grid.Column="1"
                                   Fill="{TemplateBinding Background}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Default:SharpTreeView}"
           BasedOn="{StaticResource {x:Type ListBox}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Default:SharpTreeView}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Style="{StaticResource SharpTreeViewScrollViewerStyle}"
                                      CanContentScroll="{Binding Path=(ScrollViewer.CanContentScroll), RelativeSource={RelativeSource TemplatedParent}}"
                                      HorizontalScrollBarVisibility="{Binding Path=(ScrollViewer.HorizontalScrollBarVisibility), RelativeSource={RelativeSource TemplatedParent}}"
                                      Padding="{TemplateBinding Padding}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                      VerticalScrollBarVisibility="{Binding Path=(ScrollViewer.VerticalScrollBarVisibility), RelativeSource={RelativeSource TemplatedParent}}">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="ShowRoot"
                     Value="False">
                <Setter Property="Padding"
                        Value="5 0 0 0" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="{x:Static Default:SharpTreeView.DefaultItemContainerStyleKey}"
           TargetType="{x:Type Default:SharpTreeViewItem}">
    </Style>

    <Style x:Key="{x:Static Default:SharpGridView.ItemContainerStyleKey}"
           TargetType="{x:Type ListViewItem}">
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Border Name="Bd"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="true">
                        <Border.Background>
                            <MultiBinding Converter="{Default:GridItemBackgroundConverter}">
                                <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}" />
                                <Binding Path="GridSelectedItemBackground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                <Binding Path="GridInactiveSelectedItemBackground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                <Binding Path="IsSelected" RelativeSource="{RelativeSource TemplatedParent}" />
                                <Binding Path="(Selector.IsSelectionActive)" RelativeSource="{RelativeSource TemplatedParent}" />
                            </MultiBinding>
                        </Border.Background>
                        <GridViewRowPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <TextElement.Foreground>
                                <MultiBinding Converter="{Default:GridItemForegroundConverter}">
                                    <Binding Path="Foreground" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="GridSelectedItemForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                    <Binding Path="GridInactiveSelectedItemForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                    <Binding Path="GridDisabledItemForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                    <Binding Path="IsSelected" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="(Selector.IsSelectionActive)" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="IsEnabled" RelativeSource="{RelativeSource TemplatedParent}" />
                                </MultiBinding>
                            </TextElement.Foreground>
                        </GridViewRowPresenter>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Default:SharpTreeViewItem}">
        <Setter Property="FocusVisualStyle"
                Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Default:SharpTreeViewItem}">
                    <Border Background="Transparent">
                        <Border>
                            <Border.Background>
                                <MultiBinding Converter="{Default:TreeItemBackgroundConverter}">
                                    <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="(ItemsControl.AlternationIndex)" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="(Default:SharpTreeView.ShowAlternation)" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="AlternationBackground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                    <Binding Path="IsMouseOver" RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="HoverNodeBackground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                </MultiBinding>
                            </Border.Background>
                            <Default:SharpTreeNodeView Name="nodeView"
                                                       HorizontalAlignment="Left">
                                <Default:SharpTreeNodeView.Foreground>
                                    <MultiBinding Converter="{Default:TreeNodeForegroundConverter}">
                                        <Binding Path="NodeForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                        <Binding Path="Foreground" />
                                        <Binding Path="SelectedNodeForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                        <Binding Path="DisabledNodeForeground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                        <Binding Path="IsSelected" RelativeSource="{RelativeSource TemplatedParent}" />
                                        <Binding Path="IsEnabled" RelativeSource="{RelativeSource TemplatedParent}" />
                                    </MultiBinding>
                                </Default:SharpTreeNodeView.Foreground>
                                <Default:SharpTreeNodeView.TextBackground>
                                    <MultiBinding Converter="{Default:TreeNodeTextBackgroundConverter}">
                                        <Binding Path="SelectedNodeBackground" RelativeSource="{RelativeSource AncestorType={x:Type Default:SharpTreeView}}" />
                                        <Binding Path="IsSelected" RelativeSource="{RelativeSource TemplatedParent}" />
                                    </MultiBinding>
                                </Default:SharpTreeNodeView.TextBackground>
                            </Default:SharpTreeNodeView>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Default:SharpTreeNodeView}">
        <Setter Property="Focusable"
                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Default:SharpTreeNodeView}">
                    <Grid>
                        <Default:LinesRenderer Name="linesRenderer"
                                               ClipToBounds="True"
                                               LinesBrush="{Binding LinesBrush, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}}"
                                               Visibility="{Binding ShowLines, RelativeSource={RelativeSource AncestorType={x:Type Default:SharpTreeView}}, Converter={Default:CollapsedWhenFalse}}" />
                        <StackPanel Orientation="Horizontal">
                            <FrameworkElement Name="spacer" />
                            <ToggleButton Name="expander"
                                          Style="{StaticResource ExpandCollapseToggleStyle}"
                                          IsChecked="{Binding IsExpanded}"
                                          Visibility="Hidden"
                                          Margin="0 0 7 0"
                                          VerticalAlignment="Center" />
                            <Border Name="checkBoxContainer"
                                    Width="16"
                                    Margin="0 0 3 0"
                                    Visibility="Collapsed">
                                <CheckBox IsChecked="{Binding IsChecked}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Orientation="Horizontal"
                                        Background="Transparent"
                                        ToolTip="{Binding ToolTip}">
                                <ContentPresenter Name="icon"
                                                  Content="{Binding Icon}"
                                                  Width="16"
                                                  Height="16"
                                                  Margin="0 0 5 1"
                                                  VerticalAlignment="Center"
                                                  Focusable="False">
                            		<ContentPresenter.ContentTemplate>
                                		<DataTemplate>
                            				<Image Source="{Binding}"/>
                            			</DataTemplate>
                                	</ContentPresenter.ContentTemplate>
                            	</ContentPresenter>
                                <Border Name="textContainer"
                                        Background="{TemplateBinding TextBackground}">
                                    <ContentPresenter Content="{Binding Text}"
                                                      Margin="2 0 6 0"
                                                      VerticalAlignment="Center"
                                                      Focusable="False" />
                                </Border>
                                <Border Name="textEditorContainer" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}"
                                     Value="True">
                            <Setter TargetName="textContainer"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </DataTrigger>
                    	  <DataTrigger Binding="{Binding ShowIcon}"
                                     Value="False">
                    		  <Setter TargetName="icon"
                                    Property="Visibility"
                                   Value="Collapsed" />
 						  </DataTrigger>
                        <DataTrigger Binding="{Binding IsExpanded}"
                                     Value="True">
                            <Setter TargetName="icon"
                                    Property="Content"
                                    Value="{Binding ExpandedIcon}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ShowExpander}"
                                     Value="True">
                            <Setter TargetName="expander"
                                    Property="Visibility"
                                    Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsCheckable}"
                                     Value="True">
                            <Setter TargetName="checkBoxContainer"
                                    Property="Visibility"
                                    Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsCut}"
                                     Value="True">
                            <Setter TargetName="icon"
                                    Property="Opacity"
                                    Value="0.5" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
